/**
 * Root layout placeholder.
 * All actual rendering happens under /[locale]/layout.tsx
 */

export default function RootLayout({
  children
}: {
  children: React.ReactNode;
}) {
  return children;
}
