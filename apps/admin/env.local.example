# Admin App Environment Variables
# Copia este archivo a .env.local y configura tus valores

# ============================================
# DATABASE
# ============================================
DATABASE_URL="postgresql://user:password@localhost:5432/quinielas"

# ============================================
# SPORTS PROVIDER API KEY
# ============================================
# API Key de API-Sports (para sincronización de fixtures)
# Obtén tu key en: https://dashboard.api-football.com/
SPORTS_API_KEY=your-api-key-here

# ============================================
# AUTH (Auth.js v5)
# ============================================
AUTH_URL=http://localhost:3001
AUTH_SECRET=your-secret-here-generate-with-openssl

# Email Provider (Magic Link)
EMAIL_SERVER_HOST=smtp.gmail.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=your-email@gmail.com
EMAIL_SERVER_PASSWORD=your-app-password
EMAIL_FROM=noreply@yourdomain.com

# OAuth Providers (opcional)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# ============================================
# STORAGE / MEDIA UPLOADS
# ============================================
# Provider: local | cloudinary | firebase | s3
STORAGE_PROVIDER=firebase

# Local Storage (desarrollo)
STORAGE_LOCAL_PATH=./tenants/branding
STORAGE_LOCAL_BASE_URL=/branding

# Firebase Storage
FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
# FIREBASE_CREDENTIALS={"type":"service_account",...} (opcional, para service account)

# Cloudinary (alternativa)
# CLOUDINARY_CLOUD_NAME=your-cloud
# CLOUDINARY_API_KEY=your-key
# CLOUDINARY_API_SECRET=your-secret
# CLOUDINARY_FOLDER=quinielas

# S3 / S3-compatible (alternativa)
# S3_BUCKET=your-bucket
# S3_REGION=us-east-1
# S3_ACCESS_KEY_ID=your-key
# S3_SECRET_ACCESS_KEY=your-secret
# S3_ENDPOINT= (opcional, para servicios compatibles con S3)

# ============================================
# DEVELOPMENT
# ============================================
# Tenant slug para desarrollo en localhost (opcional)
# Si no se especifica, se usa el primer tenant disponible
DEV_TENANT_SLUG=ivoka

# ============================================
# NOTAS
# ============================================
# 1. La SPORTS_API_KEY es la misma que usas en el worker
# 2. Genera AUTH_SECRET con: openssl rand -base64 32
# 3. Para producción, actualiza AUTH_URL con tu dominio real
# 4. Admin app usa puerto 3001, web app usa puerto 3000
# 5. Para Gmail, usa una App Password, no tu contraseña normal
# 6. FIREBASE_STORAGE_BUCKET: usa el valor de tu Firebase Console
# 7. Los archivos se organizan por tenant: {tenant-slug}/{kind}/{timestamp}-{filename}
# 8. Para Firebase web SDK (cliente), usa variables NEXT_PUBLIC_FIREBASE_* en .env.local
# 9. DEV_TENANT_SLUG: Solo se usa en localhost, en producción se resuelve por subdominio
